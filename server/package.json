{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate:create": "DATABASE_URL=postgresql://postgres:password@localhost:5432/job_board node-pg-migrate create",
    "migrate:up": "DATABASE_URL=postgresql://postgres:password@localhost:5432/job_board node-pg-migrate up",
    "migrate:down": "DATABASE_URL=postgresql://postgres:password@localhost:5432/job_board node-pg-migrate down"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "express": "^5.2.1",
    "node-pg-migrate": "^8.0.4",
    "pg": "^8.19.0",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/pg": "^8.18.0"
  }
}
